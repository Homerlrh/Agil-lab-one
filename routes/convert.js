var express = require('express');
var router = express.Router();

/* GET convert request */
router.get('/convert', function(req, res, next) {
  //respond with convert result
  res.send();
});

module.exports = router;
